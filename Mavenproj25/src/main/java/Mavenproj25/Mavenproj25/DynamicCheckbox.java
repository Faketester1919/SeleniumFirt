package Mavenproj25.Mavenproj25;

import java.awt.List;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.Select;
import org.testng.annotations.Test;
public class DynamicCheckbox {
/*@Test
	//Handling dynamic element
    driver.get("https://www.google.com");
	List<WebElement> checkbox = driver.findElement(By.xpath("Your xpath here"));
	for(int i=0; i<check.size(); i++)        
	    {
		//traversing through the loop of all checkboxes
	       WebElement localradio = check.get(i);
	       //"value" is no of checkbox text name
	       String val= localradio.getAttribute("value");
	       System.out.println(val);
	       //java is the name of checkbox dynamically changing
	       if (val.equalsIgnoreCase("java"))
	       {
	    	 localradio.click();  
	       }
	    }*/

}



